# Внимание!
# - .gitignore работает в соответствии с спецификацией glob
# - Порядок выражений имеет значение
# - Каждый символ в выражениях имеет значение

# Файлы IDEA
dependency-reduced-pom.xml
*.iml
/target/**
*/target/**
/out/**
/.idea/*

# Тестовые папки
# Советую их пометить, как Excluded, чтобы IDEA их не индексировала: ПКМ по папке -> Mark Directory as -> Excluded
/Test?*/*

# Спец. файлы для создания пустых папок
!/**/.gitkeep